using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Inbrand.Models
{
    public class SubscriptionOperations
    {
        //public List<Subscription> GetSubscriptions(Subscription subModel)
        //{
        //    var SubscriptionList = new List<Subscription>();

        //    //using (var context = new ApplicationDbContext())
        //    //{
        //    //    // Query for all blogs with names starting with B 
        //    //    SubscriptionList = (from b in context.Subscriptions
        //    //                        where (subModel.customerID.Length > 0 && b.customerID.StartsWith(subModel.customerID)) || (subModel.customerName.Length > 0 && b.customerName.StartsWith(subModel.customerName)) || (subModel.domainName.Length > 0 && b.domainName.StartsWith(subModel.domainName))
        //    //                        || (subModel.tld.Length > 0 && b.tld.StartsWith(subModel.tld)) || (subModel.articleID.Length > 0 && b.articleID.StartsWith(subModel.articleID)) || (b.nextBillDate == subModel.nextBillDate)
        //    //                        select b).ToList();

        //    //}
        //    return SubscriptionList;
        //}
    }
}